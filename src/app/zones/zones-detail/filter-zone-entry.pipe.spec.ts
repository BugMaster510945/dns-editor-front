import { FilterZoneEntryPipe } from './filter-zone-entry.pipe';

describe('FilterZoneEntryPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterZoneEntryPipe();
    expect(pipe).toBeTruthy();
  });
});
