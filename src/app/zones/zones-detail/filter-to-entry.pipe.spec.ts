import { FilterToEntryPipe } from './filter-to-entry.pipe';

describe('FilterToEntryPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterToEntryPipe();
    expect(pipe).toBeTruthy();
  });
});
